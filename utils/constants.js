/**
 * Subscription Plan Types
 * NOTE: States and Sectors are now dynamically managed via master collections.
 * Do NOT add hardcoded values here - use the admin panel to manage them.
 */
const PLAN_TYPES = {
  PLAN_1: 'PLAN_1',
  PLAN_2: 'PLAN_2',
  PLAN_3: 'PLAN_3'
};

module.exports = {
  PLAN_TYPES
};